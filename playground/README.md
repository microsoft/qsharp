# Playground

This is a simple web site built using the Monaco editor and the qsharp npm package.

Build the entire repo by running `./build.py` in the root directory. Then make
`./playground` your current directory and run `npm start` to start the web server.
