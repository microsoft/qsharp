namespace Kata {
    import Std.Math.*;

    function EigenvaluesS() : Complex[] {
        // Replace the return value with correct answer.
        return [
            Complex(0.0, 0.0),
            Complex(0.0, 0.0)
        ];
    }
}
