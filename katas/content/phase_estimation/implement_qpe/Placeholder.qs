namespace Kata {
    operation PhaseEstimation(
        U : Qubit => Unit is Ctl, 
        P : Qubit => Unit,
        n : Int
    ) : Int {
        // Implement your solution here...

        return 0;
    }
}
