namespace Kata {
    function PowersOfI(n : Int) : Int {
        // Replace the return values with correct answers.
        if n % 4 == 0 {
            return 0;
        } else {
            return 0;
        }
   }
}
